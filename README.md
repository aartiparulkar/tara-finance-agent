## Installation Guide
```js
1. npm init -y
2. npm install express pg dotenv zod cors pino helmet morgan uuid
3. npm install -D typescript ts-node-dev @types/node @types/express @types/cors @types/morgan eslint prettier vitest
4. npx tsc --init
5. npm i --save-dev @types/pg
6. npm install -D tsx
```